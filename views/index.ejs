<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style/main.css">
</head>
<body>
    <h1>SIMPLE TO DO APPLICATION</h1>


    <%-include("add-data.ejs") %>
    <% if(typeof error !== undefined&& error !=="empty") { %>
        <%-include("error.ejs") %> 
    <% } %>








    <a href="/?page=1">Sort by (A-Z)</a>
    <a href="?page=-1">Sort by (Z-A)</a>    

    <% for(let i=0; i <data.length; i++) { %>
    
        <div>  <%= data[i].name %>
            <span>
                date:
                 <%=  data[i].date %>
             </span>

                
            <a href="/edit/<%= data[i]._id %>"  > EDIT </a> 

            <a href="/delete/<%= data[i]._id %>"  > DELETE </a>
                 

        </div>
    
    
    <% } %>

     
</body>
</html>